{
  "name": "frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && vite build",
    "preview": "vite preview",
    "start": "rimraf dist && vite build --watch"
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "toastify-js": "^1.12.0"
  }
}
